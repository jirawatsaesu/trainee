type District {
  id: ID
  name_th: String
  name_en: String
  province_id: String
  created_at: String
  updated_at: String
}

type Query {
  district(name: String, province_id: String): [District]
}
